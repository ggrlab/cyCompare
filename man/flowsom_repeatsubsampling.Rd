% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flowsom_repeatsubsampling.R
\name{flowsom_repeatsubsampling}
\alias{flowsom_repeatsubsampling}
\title{Train FlowSOM Using Repeated Subsampling}
\usage{
flowsom_repeatsubsampling(
  ff_list,
  outdir,
  columns_clustering = c("FITC-A", "PE-A", "ECD-A", "PC5.5-A", "PC7-A", "APC-A",
    "AF700-A", "PB-A"),
  n_subsampling = 1,
  n_subsampled_cells = 10000,
  subsampling_seed_first = 427764,
  ...
)
}
\arguments{
\item{ff_list}{A list or \code{flowSet} of flowFrames containing the training data.}

\item{outdir}{Optional output directory to save FlowSOM results. If \code{NULL}, results are returned only in memory.}

\item{columns_clustering}{Character vector of marker channels to use for clustering.}

\item{n_subsampling}{Integer; number of repeated subsampling iterations. Default: 1.}

\item{n_subsampled_cells}{Integer; number of cells per subsample. Will up/downsample automatically.}

\item{subsampling_seed_first}{Integer; base seed used to ensure reproducibility of each subsampling.}

\item{...}{Additional arguments passed to \code{cytobench::flowSOM_optimal()}.}
}
\value{
The output of \code{cytobench::flowSOM_optimal()}, typically a list containing the FlowSOM model and cluster assignments.
}
\description{
This function performs FlowSOM clustering after repeated subsampling of a flow cytometry training set.
Each subsample is taken independently using a reproducible seed and concatenated into one training set
for FlowSOM model fitting.
}
\examples{
\dontrun{
result <- flowsom_repeatsubsampling(
    ff_list = some_flowset,
    outdir = tempdir(),
    columns_clustering = c("CD3", "CD4", "CD8"),
    n_subsampling = 3,
    n_subsampled_cells = 5000
)
}
}
