% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ROC_grouped.R
\name{plot_ROC_grouped}
\alias{plot_ROC_grouped}
\title{Plot ROC Curves Grouped by Metadata for Multiple Models and Outcomes}
\usage{
plot_ROC_grouped(
  calced_roc,
  dfcol_grouping_supersamples = c("Study"),
  dfcol_grouping_samples = "Device",
  dfcol_outcomes = c("outcome_1", "outcome_2"),
  dfcol_train_validation_other = "train_validation_test",
  dv_class_positive = c(outcome_1 = "A", outcome_2 = 5.1),
  device_colors = c(Fortessa = "#FF0000", Cytoflex = "#00FF00", Aurora = "#0000FF")
)
}
\arguments{
\item{calced_roc}{A list with \code{results} and \code{groups} entries, where \code{results} contains ROC objects
and \code{groups} describes the training configuration for each result.}

\item{dfcol_grouping_supersamples}{Character vector of column names used to group across supersamples (e.g., \code{"Study"}).}

\item{dfcol_grouping_samples}{Character scalar naming the sample grouping column (e.g., \code{"Device"}).}

\item{dfcol_outcomes}{Character vector of column names corresponding to different outcome types (e.g., \code{"outcome_1"}, \code{"outcome_2"}).}

\item{dfcol_train_validation_other}{Character scalar for column indicating \code{"train"}, \code{"validation"}, or \code{"test"} set.}

\item{dv_class_positive}{A named vector indicating the positive class for each outcome variable.}

\item{device_colors}{A named character vector assigning colors to device names.}
}
\value{
A \code{data.frame} (tibble) where each row contains:
\itemize{
\item Grouping metadata (e.g., \code{"train.Device"}, \code{"train.Study"}, ...)
\item Outcome/model/clustering info
\item ROC plot objects (\code{roc.train}, \code{roc.validation}, \code{roc.test}, etc.) as \code{ggplot} objects.
}
}
\description{
This function takes a structured ROC result object (as returned by your pipeline)
and generates grouped ROC plots for each model/outcome combination. ROC curves are grouped
by training configuration and colored using the specified device colors.
}
