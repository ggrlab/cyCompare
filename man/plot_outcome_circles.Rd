% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_outcome_circles.R
\name{plot_outcome_circles}
\alias{plot_outcome_circles}
\title{Plot Outcome Proportions by Supersample Groupings as Circular Bar Charts}
\usage{
plot_outcome_circles(df, dfcol_grouping_supersamples, dfcol_outcomes)
}
\arguments{
\item{df}{A \code{data.frame} containing both the grouping and outcome columns.}

\item{dfcol_grouping_supersamples}{A character vector specifying the column names
to be used for creating supersample groups via interaction.}

\item{dfcol_outcomes}{A character vector specifying the column names of the
outcome variables to be plotted.}
}
\value{
A named list of \code{ggplot} objects (one per outcome), each displaying
a radial bar chart of outcome frequencies grouped by supersample combination.
}
\description{
This function creates circular bar plots for one or more outcome variables,
grouped by combinations of columns (supersample groupings). Each plot shows
the distribution of outcome values across the grouping.
}
\examples{
\dontrun{
df <- data.frame(
    batch = rep(c("A", "B"), each = 50),
    device = rep(c("X", "Y"), times = 50),
    outcome1 = sample(c("pos", "neg"), 100, replace = TRUE),
    outcome2 = sample(c("high", "low"), 100, replace = TRUE)
)
plots <- plot_outcome_circles(df, c("batch", "device"), c("outcome1", "outcome2"))
plots[["outcome1"]]
}

}
