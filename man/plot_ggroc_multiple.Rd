% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ggroc_multiple.R
\name{plot_ggroc_multiple}
\alias{plot_ggroc_multiple}
\title{Plot multiple ROC curves with ggplot2}
\usage{
plot_ggroc_multiple(
  named_proc_list,
  col_or_linetype = c("linetype", "color"),
  geom_line_args = NULL
)
}
\arguments{
\item{named_proc_list}{Named list of \code{pROC::roc} objects. Names will be used in the legend and appended with AUC and CI.}

\item{col_or_linetype}{Character vector of length 1, either \code{"linetype"} or \code{"color"}.
Determines whether ROC curves are distinguished by line type or color.}

\item{geom_line_args}{List of additional arguments passed to \code{ggplot2::geom_line()} (e.g., \code{list(size = 1)}).}
}
\value{
A named list with three ggplot2 objects:
\describe{
\item{plot}{Full ROC plot with legend}
\item{onlyROC}{ROC plot without the legend}
\item{onlyLEGEND}{Legend as a standalone plot}
}
}
\description{
This function generates a combined ROC plot from a named list of \code{pROC::roc} objects.
It allows customization by mapping the curve identity to either color or linetype and
supports additional arguments passed to \code{geom_line}.
}
\details{
The function returns a list containing:
\itemize{
\item A full ROC plot with legend
\item A ROC plot without the legend
\item A standalone ggplot object for the legend
}
}
\examples{
data(aSAH, package = "pROC")
r1 <- pROC::roc(aSAH$outcome, aSAH$s100b, levels = c("Good", "Poor"), direction = "<")
r2 <- pROC::roc(aSAH$outcome, -aSAH$s100b, levels = c("Good", "Poor"), direction = "<")
r3 <- pROC::roc(
    aSAH$outcome,
    aSAH$s100b + rnorm(nrow(aSAH), 0, .1),
    levels = c("Good", "Poor"),
    direction = "<"
)
named_proc_list <- list(
    "ROC_1" = r1,
    "ROC_2" = r2,
    "ROC_3" = r3
)

print(plot_ggroc_multiple(
    named_proc_list,
    col_or_linetype = "color", geom_line_args = list(size = 1)
))
print(plot_ggroc_multiple(named_proc_list))
print(plot_ggroc_multiple(
    named_proc_list,
    col_or_linetype = "linetype", geom_line_args = list(size = 1)
))
}
