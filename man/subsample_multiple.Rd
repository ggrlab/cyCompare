% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subsample_multiple.R
\name{subsample_multiple}
\alias{subsample_multiple}
\title{Perform Repeated Subsampling of Flow Cytometry Data}
\usage{
subsample_multiple(
  ff_list,
  n_subsampling = 1,
  n_subsampled_cells = 10000,
  subsampling_seed_first = 427764
)
}
\arguments{
\item{ff_list}{A list or \code{flowSet} of \code{flowFrame} objects to be subsampled.}

\item{n_subsampling}{Integer. Number of repeated subsampling rounds to perform (default: 1).}

\item{n_subsampled_cells}{Integer. Number of cells per subsample. Automatically downsamples or upsamples.}

\item{subsampling_seed_first}{Integer. Base seed used for reproducibility across iterations.}
}
\value{
A list of subsampled \code{flowFrame} objects, one per sample per iteration. Sample names are suffixed accordingly.
}
\description{
Applies repeated subsampling to a list or \code{flowSet} of flowFrames. Each subsample iteration
uses a reproducible seed and appends a unique \verb{_subsampledX} suffix to the sample names.
}
\examples{
\dontrun{
subsampled_frames <- subsample_multiple(
    ff_list = my_flowset,
    n_subsampling = 3,
    n_subsampled_cells = 5000,
    subsampling_seed_first = 123
)
}
}
